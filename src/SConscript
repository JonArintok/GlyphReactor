flags = "-Wall -g -std=gnu11"
env = Environment(CC = "gcc", CCFLAGS = flags)
src = [
	"main.c",
	"oglTools.c",
	"fileTools.c",
	"misc.c",
	"optionsAndErrors.c",
	"timestamp.c"
]
env.Program("GlyphReactor", src, LIBS = ["GL", "GLEW", "SDL2", "m"])
